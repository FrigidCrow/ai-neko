# ai-neko 工程规则

## 目标与边界

- 本工程是独立 AI 伴侣项目，根目录 `/Users/frigidcrow/Dev/ai-neko`，工作名 ai-neko。
- 用户已选定 LangGraph 作为对话编排。首要平台 Windows 11 x64；记忆本地保存，允许调用配置的云模型。
- `/Users/frigidcrow/Dev/neko-companion` 仅为只读源码与教程参考；不得借用其配置、运行数据、凭据、服务端口或 `.venv`。本工程不是该仓库的 worktree，也不是另一份完整 N.E.K.O 聊天系统。
- 已完成独立工程初始化、改名为 ai-neko、技能调研及计划/模块图解。用户于 2026-09-25 授权开始第一阶段 M0；实现以 `docs/PLAN.md` 为准，材料通过不代表功能已实现。
- 用户当前优先需要查攻略/搜索资料：M1 必须接入真实搜索与网页正文读取、带来源回答；键鼠自动操作、电脑控制和接管其他软件当前不做。正常桌宠拖拽、语音与用户主动提供截图不受此排除影响。

## 架构与复用

- LangGraph 是唯一对话决策中心。媒体与连接模块管理任务、取消和播放，不另建完整工具循环。
- 优先复用可获得且许可适用的 N.E.K.O 组件，提取所需边界；不得直接导入参考仓库并触发其生产初始化。
- 长期记忆以本工程 Memory Service 为唯一权威。可移植已有记忆实现；checkpoint 只保存会话执行状态，不能冒充完整事实/人格记忆。
- 窗口与托盘的原版完整实现来自独立 N.E.K.O.-PC 工程；访问、许可与桥接未核实前，不承诺直接使用。若无法复用，只补必要桌面宿主，继续复用已核实的其他组件。
- 导入代码或素材时逐项记录来源 commit、原路径、许可证、改动和升级方式，保留要求的 LICENSE/NOTICE。当前不复制第三方运行代码和素材。

## 开发与验收

- 实现前更新 `docs/PLAN.md` 的任务和验收项；实际命令写 `WORKLOG.md`，证据与未完成项写 `REVIEW.md`。
- 源码、依赖、计划、测试和构建均在本项目维护。进入本目录后再执行开发命令。
- 独立应用身份、配置、数据根、备份、日志和端口发现机制要早于首次真实启动落地；禁止自动读取或迁移原版资料。
- 默认使用合成测试资料；记录运行环境与实际依赖锁版本。真实模型测试和确定性测试分开计数。
- Mac 上的静态检查与实验不能证明 Windows 打包、托盘、音频或桌宠可用。Windows 产物记录源 commit、构建环境、摘要及真机结果。
- Pending、Failed 或 Blocked 不写成完成；不把图暂停等同于停止音频、不把重放成功等同于副作用只执行一次。
- 未要求时不创建远程仓库、不推送、不发布产物、不写全局记忆。
